query GetTransactionHistoryData($address: ID!) {
  account(id: $address) {
    id
    transactions(orderBy: timestamp, orderDirection: desc, first: 25) {
      transaction {
        ...Transaction
      }
    }
  }
}
