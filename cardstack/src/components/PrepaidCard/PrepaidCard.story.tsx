import { storiesOf } from '@storybook/react-native';
import React from 'react';

import { PrepaidCard } from './PrepaidCard';

storiesOf('Prepaid Card', module).add('Default', () => <PrepaidCard />);
